const PORT = process.env.PORT || 3000;
const SETTINGS = {
    defaultOrbs: 5000,
    defaultKillers: 100,
    defaultSpeed: 8,
    defaultSize: 30,
    defaultZoom: 1.5,
    worldWidth: 5000,
    worldHeight: 5000,
  }

export { PORT , SETTINGS}